function RS_timer
    total_duration = 300;  % Total duration in seconds

    % Display the start message
    disp('5 minutes timer started.');

    % Wait for 5 minutes
    pause(total_duration);

    % Display the finish message
    disp('5 minutes timer finished.');
end